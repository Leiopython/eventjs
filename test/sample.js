var chai = require('chai')
  , expect = chai.expect
  , should = chai.should();

describe('Array', function() {
  describe('#indexOf()', function() {
    
    it('should return -1 when the value is not present', function() {
      expect([1,2,3].indexOf(4)).to.equal(-1);
    });

    it('it should show as pending test');
    
  });
});